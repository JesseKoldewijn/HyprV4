set -U fish_greeting ""

# Commands
    # NodeJS
    # alias np="npm $*"
    # alias pn="pnpm $*"
    # alias yr="yarn $*"
    # Git
    alias g="git $*"
    alias gc="git commit -m $*"
    # System
    alias la="ls -la $*"